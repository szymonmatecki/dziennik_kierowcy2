
import 'package:flutter/material.dart';


class AddWorkDayContent extends StatelessWidget {
  const AddWorkDayContent({
    Key? key,
  }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Dwa'),
    );
  }
}
